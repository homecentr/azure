name: Deploy master
on:
  push:
    branches:
      - master

jobs:
  deploy_prod:
    uses: ./.github/workflows/partial-deploy.yml
    with:
      environment: prod
      githubRelease: false
    secrets:
      aad_credentials: ${{ secrets.AZUREAD_CREDENTIALS_LAB }}
      terraform_token: ${{ secrets.TERRAFORM_CLOUD_TOKEN }}
      cloudflare_api_token: ${{ secrets.CLOUDFLAREAPI_TOKEN }}